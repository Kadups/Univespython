def exercio():
  palavra = input('Digite uma palavra ')
  lista = list(palavra)
  segredo = ''
  for x in lista:
    if (x =='z'): segredo += 'a'
    else: segredo += chr(ord(x)+1)
  print(segredo)  

exercio()
